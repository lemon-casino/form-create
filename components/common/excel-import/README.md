# Excel Import Component

This component provides a multi-step dialog for importing Excel files. It allows
users to select a file, preview data, configure field mappings and finally
import the data into a table form.

```js
import ExcelImport from '@form-create/component-excel-import'
```
